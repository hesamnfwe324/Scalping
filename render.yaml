services:
  # ─── mt5rest Docker bridge ─────────────────────────────────────────────────
  - type: web
    name: goldscalper-mtapi
    runtime: docker
    dockerfilePath: ./mtapi-bridge/Dockerfile
    plan: free
    healthCheckPath: /Ping
    # Only redeploy when mtapi-bridge files actually change.
    # Without this filter, every Python commit (live_trading/, telegram_panel/)
    # triggers an unnecessary Docker redeploy that restarts Wine/MT5 from
    # scratch — the primary cause of the repeated "Failed deploy" status.
    paths:
      - mtapi-bridge/**
    envVars:
      - key: RENDER_EXTERNAL_URL
        value: "https://goldscalper-mtapi.onrender.com"
      - key: MT5_HOST
        value: "AMarkets-Demo"
      - key: MT5_PORT
        value: "443"
      - key: MT5_USER
        sync: false
      - key: MT5_PASSWORD
        sync: false

  # ─── Redis — cross-service IPC (robot ↔ Telegram panel) ───────────────────
  # FIX: must be in the same region as the robot/panel web services (oregon).
  # It was previously created in frankfurt, so its private hostname never
  # resolved from oregon — the panel silently never saw live position/state
  # updates from the robot even though the robot itself was healthy.
  - type: redis
    name: goldscalper-redis
    plan: free
    region: oregon
    ipAllowList: []   # private — Render internal routing only; no public access

  # ─── Trading robot ─────────────────────────────────────────────────────────
  - type: web
    name: goldscalper-v4-robot
    runtime: python
    plan: free
    healthCheckPath: /health
    buildCommand: pip install -r live_trading/requirements.txt
    startCommand: python live_trading/server.py
    envVars:
      - key: RENDER_EXTERNAL_URL
        value: "https://goldscalper-v4-robot.onrender.com"
      - key: MTAPI_URL
        value: "https://goldscalper-mtapi.onrender.com"
      - key: MT5_HOST
        value: "AMarkets-Demo"
      - key: MT5_PORT
        value: "443"
      - key: MT5_USER
        sync: false
      - key: MT5_PASSWORD
        sync: false
      # FIX: broker reports gold as "XAUUSD", not "XAUUSDb" — the wrong
      # symbol caused every candle/order request to fail silently, which was
      # the real reason the robot never opened a trade.
      - key: SYMBOL
        value: XAUUSD
      # FIX: TIMEFRAME and CANDLE_WINDOW are now env-configurable (fix #4).
      - key: TIMEFRAME
        value: "5m"
      - key: CANDLE_WINDOW
        value: "300"
      - key: RISK_PERCENT
        value: "1.0"
      # ─── Risk / Signal thresholds (PRODUCTION values) ─────────────────────
      # CRITICAL FIX: These were set to "testing mode" values (1 / 0 / 50 / 50)
      # that effectively disabled every protection layer. Restored to production
      # defaults from config.py so Guardian and quality filters work correctly.
      #
      #   MIN_CONFIRMATIONS : at least 3 of 4 engines (SMC, Trend, PA, Wyckoff)
      #                        must agree before a trade is allowed.
      #   CONF_HARD_MIN     : trades below 35% confidence are always rejected,
      #                        regardless of confirmations.
      #   DAILY_LOSS_LIMIT_PCT : Guardian halts trading if intraday P&L drops
      #                        more than 3% below session-open balance.
      #   MAX_DRAWDOWN_PCT  : Guardian halts if equity drops more than 8%
      #                        below the session equity peak.
      - key: MIN_CONFIRMATIONS
        value: "3"
      - key: CONF_HARD_MIN
        value: "35.0"
      - key: DAILY_LOSS_LIMIT_PCT
        value: "3.0"
      - key: MAX_DRAWDOWN_PCT
        value: "8.0"
      - key: SLIPPAGE_POINTS
        value: "30"
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: STATE_FILE
        value: "/tmp/robot_state.json"
      - key: MT5_SNAPSHOT
        value: "/tmp/robot_mt5_snapshot.json"
      - key: COMMANDS_FILE
        value: "/tmp/robot_commands.json"
      - key: GUARDIAN_STATE_FILE
        value: "/tmp/guardian_state.json"
      - key: LOG_FILE
        value: "/tmp/robot.log"
      - key: REDIS_URL
        fromService:
          type: redis
          name: goldscalper-redis
          property: connectionString
      # FIX #1: ROBOT_COMMAND_TOKEN enables HTTP command fallback from panel.
      - key: ROBOT_COMMAND_TOKEN
        sync: false

  # ─── Telegram control panel ────────────────────────────────────────────────
  - type: web
    name: goldscalper-v4-panel
    runtime: python
    plan: free
    healthCheckPath: /health
    buildCommand: pip install -r telegram_panel/requirements.txt
    startCommand: python telegram_panel/server.py
    envVars:
      - key: RENDER_EXTERNAL_URL
        value: "https://goldscalper-v4-panel.onrender.com"
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_OWNER_ID
        sync: false
      - key: PANEL_ENCRYPTION_KEY
        sync: false
      - key: TELEGRAM_ADMIN_IDS
        value: ""
      - key: PYTHON_VERSION
        value: "3.11.0"
      # NOTE: /tmp/panel.db is wiped on every Render free-tier restart (ephemeral
      # filesystem). This loses trade history, sessions, and audit logs stored in
      # SQLite. Critical trading state (Guardian, robot status) survives through
      # Redis. To make the panel DB persistent, upgrade to a paid Render plan,
      # mount a Render Disk at /data, and change this to /data/panel.db.
      - key: PANEL_DB_PATH
        value: "/tmp/panel.db"
      - key: MT5_USER
        sync: false
      - key: MT5_PASSWORD
        sync: false
      - key: MT5_HOST
        value: "AMarkets-Demo"
      - key: MT5_ACCOUNT_TYPE
        value: "demo"
      - key: LOG_LEVEL
        value: "INFO"
      - key: ROBOT_INTERFACE_MODE
        value: "redis"
      - key: ROBOT_STATE_PATH
        value: "/tmp/robot_state.json"
      # FIX #1: ROBOT_BASE_URL and ROBOT_COMMAND_TOKEN enable HTTP fallback.
      - key: ROBOT_BASE_URL
        value: "https://goldscalper-v4-robot.onrender.com"
      - key: ROBOT_COMMAND_TOKEN
        sync: false
      - key: REDIS_URL
        fromService:
          type: redis
          name: goldscalper-redis
          property: connectionString
