# GoldScalperPro v4 — Live Trading Engine Dependencies
    # Exact version pins for reproducible, deterministic builds.
    #
    # Install:
    #   pip install -r live_trading/requirements.txt
    #
    # Verify:
    #   python -c "import aiohttp, aiofiles; print('OK')"
    #
    # Audit date: 2026-07-22

    # ── Async HTTP — REST client for mtapi.io bridge ──────────────────────────────
    aiohttp==3.9.5

    # ── Async file writes — used for atomic state file updates ───────────────────
    aiofiles==23.2.1

    # ── Redis — cross-service IPC between robot and Telegram panel on Render ──────
    # Required when REDIS_URL is set (separate Render services cannot share files).
    redis==5.2.1
    